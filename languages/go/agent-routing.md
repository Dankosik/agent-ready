- Go: when gopls MCP is available, start with `go_workspace`; use `go_symbol_references` and `go_diagnostics` for build-aware correctness, file/package context for targeted understanding, and `go_vulncheck` only for dependency/security work. Use CodeGraph when available for graph/impact, `ast-grep` for code shapes, `rg` for literals/files, and `go_search` only as a fuzzy fallback.
